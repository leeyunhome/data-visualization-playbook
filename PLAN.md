# 데이터 시각화 포트폴리오 계획

인프런 [32가지 데이터 시각화 전략](https://www.inflearn.com/course/32-data-visualizatio) 수강과 병행하여
**이직용 GitHub Pages 포트폴리오**를 만든다.

- 목적: 이직 / 커리어 전환
- 데이터: 공개 데이터셋 위주 (재현 가능)
- 산출물: GitHub Pages 정적 사이트 1개 링크

---

## 0. 전제와 원칙

**채용 담당자는 5분 이상 안 본다.** 그래서 이 포트폴리오의 성공 기준은 "32가지 기법을 다 해봤다"가 아니라
**"이 사람은 데이터를 보고 결론을 낼 줄 안다"가 링크 하나로 전달되는 것**이다.

이 기준에서 나오는 3가지 원칙:

1. **강의 노트 ≠ 포트폴리오.** 강의를 따라간 기록은 학습 트랙에 두고, 공개 사이트에는 스스로 문제를 정의하고
   푼 케이스 스터디를 올린다. 강의 노트는 사이트에 "학습 기록" 섹션으로 곁들이는 정도.
2. **차트 제목은 결론 문장으로.** "월별 전력 사용량" (X) → "여름 피크는 겨울의 2.3배, 7~8월에 집중" (O).
   기법을 보여주는 게 아니라 기법으로 알아낸 것을 보여준다.
3. **도메인 서사를 하나로.** 흩어진 예제 10개보다 "설비·공정·에너지 데이터를 읽는 사람"이라는
   일관된 인상 3개가 강하다. 아래 케이스 스터디 3개는 이 서사로 묶여 있다.

---

## 1. 두 트랙 구조

| | 학습 트랙 | 포트폴리오 트랙 |
| --- | --- | --- |
| 위치 | `data_visualization_inflearn/` (기존) | `data_visualization/` (여기) |
| 내용 | 강의 27강 따라가기, 기법 실습 | 케이스 스터디 3개 + 기법 갤러리 |
| 공개 | 선택 (공개해도 무방) | **GitHub Pages 공개** |
| 독자 | 나 자신 | 채용 담당자 |

학습 트랙에서 배운 기법이 포트폴리오 트랙의 갤러리·케이스로 흘러 들어간다. 한 방향으로만 흐르게 해서
같은 코드를 두 곳에서 고치는 상황을 만들지 않는다.

> **기존 폴더 처리:** `data_visualization_inflearn`은 커밋이 0개다. 첫 커밋을 만들어 두고
> (nbstripout이 이미 걸려 있어 출력은 자동으로 벗겨짐) 그대로 학습 트랙으로 쓴다.
> 리모트는 `dataviz-course-notes` 정도로 별도 저장소를 파거나, 로컬로만 둬도 된다.

---

## 2. 기술 스택

| 항목 | 선택 | 이유 |
| --- | --- | --- |
| 사이트 빌더 | **Quarto** | `.ipynb`를 그대로 렌더. 검색·다크모드·반응형 기본 제공. `quarto publish gh-pages` 한 줄 배포 |
| 파이썬 환경 | **uv** + Python 3.12 | 이미 설치됨. `uv.lock`으로 재현성 보장 |
| 정적 차트 | matplotlib + seaborn | 강의와 동일. 인쇄/썸네일용 |
| 인터랙티브 차트 | **plotly** | 정적 호스팅에서도 HTML에 그대로 임베드되어 동작 |
| 모델링 | scikit-learn, umap-learn | PCA / t-SNE / UMAP / K-means |
| 배포 | GitHub Actions → gh-pages | 푸시하면 자동 렌더·배포 |

**Streamlit은 제외한다.** GitHub Pages는 정적 호스팅이라 파이썬 서버를 못 띄운다.
인터랙티브가 필요한 부분은 Plotly로 처리하면 정적 사이트에서도 줌·호버·필터가 다 된다.

**설치 필요:** Quarto (`winget install --id Posit.Quarto`). 나머지는 uv로 해결.

---

## 3. 저장소 / 사이트 구조

```
data_visualization/
├── _quarto.yml              사이트 설정 (네비게이션, 테마, 렌더 대상)
├── index.qmd                랜딩 — 한 줄 소개 + 케이스 카드 3개
├── about.qmd                이력 요약, 기술 스택, 연락처
├── cases/
│   ├── 01-bike-demand.ipynb     케이스 1
│   ├── 02-power-anomaly.ipynb   케이스 2
│   └── 03-secom-process.ipynb   케이스 3
├── gallery/
│   ├── index.qmd            "언제 어떤 차트를 쓰나" 의사결정 가이드
│   ├── 1d.ipynb             1차원 기법
│   ├── 2d.ipynb             2차원 기법
│   ├── timeseries.ipynb     시계열·센서
│   ├── highdim.ipynb        클러스터링·차원축소
│   └── etc.ipynb            워드클라우드·생키·트리맵
├── notes/                   강의 학습 기록 (학습 트랙에서 정제해 옮김)
├── src/
│   ├── theme.py             공통 스타일·팔레트·한글 폰트
│   └── datasets.py          데이터 다운로드 + 캐시 (원본은 커밋 안 함)
├── data/                    .gitignore (스크립트로 재생성)
├── pyproject.toml / uv.lock
├── .github/workflows/publish.yml
└── README.md                사이트 링크 + 썸네일 3개 + 한 줄 결론씩
```

`README.md`와 `index.qmd`는 **같은 내용을 두 형태로** 유지한다. GitHub 저장소로 들어온 사람과
사이트 링크로 들어온 사람 둘 다 3초 안에 무엇인지 알아야 한다.

---

## 4. 케이스 스터디 3개

셋 다 UCI / 공공 공개 데이터라 누구나 재현 가능하고, "설비·공정·에너지"라는 하나의 서사로 묶인다.
강의 커리큘럼의 세 축(1D·2D 기초 / 시계열·센서 / 고차원·차원축소)에 각각 대응한다.

### 케이스 1 — 수요 예측을 위한 탐색적 분석
- **데이터:** Seoul Bike Sharing Demand (UCI, 8760행 × 14열)
- **질문:** 대여량을 결정하는 진짜 변수는 무엇인가? 기온인가, 시간대인가, 그 상호작용인가?
- **쓰는 기법:** 히스토그램/KDE, ECDF, Box·Violin, Scatter+Regression, Hexbin, 상관 히트맵, Slope
- **결과물:** "언제 자전거가 부족한가"를 한 장으로 답하는 차트 + 운영 제안 3줄
- **대응 강의:** 섹션 2-3(1차원), 2-4(2차원)

### 케이스 2 — 센서 시계열에서 이상 구간 찾기
- **데이터:** UCI Individual Household Electric Power Consumption (200만 행, 분 단위 4년)
- **질문:** 정상 패턴을 어떻게 정의하고, 벗어난 구간을 어떻게 눈에 보이게 만드나?
- **쓰는 기법:** 리샘플링, 이동평균/이동표준편차, 계절 분해, 캘린더 히트맵, 잔차 플롯, 스파크라인
- **결과물:** 4년치를 한 화면에 압축한 캘린더 히트맵 + 이상 구간 top-5 확대 뷰 (Plotly 인터랙티브)
- **대응 강의:** 섹션 2-5(시계열/센서)
- **어필 포인트:** 200만 행을 다루는 성능 감각 (다운샘플링, 벡터화)

### 케이스 3 — 590개 센서, 고차원 공정 데이터 읽기
- **데이터:** UCI SECOM (반도체 공정, 1567행 × 590 센서, 불량률 6.6%)
- **질문:** 센서가 590개일 때 사람은 무엇을 봐야 하나? 불량은 어떤 센서 조합에서 나오나?
- **쓰는 기법:** 결측 패턴 시각화, PCA + biplot, t-SNE, UMAP, K-means, Clustered Heatmap
- **결과물:** 590차원 → 2D 지도 위에 불량이 어디 모여 있는지, 기여 센서 top-10
- **대응 강의:** 섹션 2-6(복잡 데이터), 2-7(차원 축소)
- **어필 포인트:** 결측·클래스 불균형 같은 실제 데이터의 지저분함을 회피하지 않고 다룸

> 셋 다 "차트를 그렸다"가 아니라 **"그래서 무엇을 하면 되는가"**로 끝나야 한다.
> 각 케이스 마지막 섹션 제목은 고정: `## 그래서, 무엇을 해야 하는가`

---

## 5. 기법 갤러리

강의의 32가지 기법을 **의사결정 가이드** 형태로 만든다. 단순 나열이 아니라
"내 데이터가 이럴 때는 이 차트" 진입점을 먼저 두고, 각 기법마다:

- 언제 쓰나 / 언제 쓰면 안 되나 (한 줄씩)
- 최소 재현 코드 (10줄 이내)
- 결과 차트
- 흔한 실수 1개

이 페이지는 포트폴리오에서 **"이 사람 레퍼런스 만들 줄 아네"**를 담당하고,
동시에 본인이 실무에서 계속 열어볼 자산이 된다.

---

## 6. 일정 (7주 / 주 4~5시간 기준)

강의 총 3시간 32분이라 강의 자체는 2~3주면 끝난다. 나머지는 포트폴리오 제작 시간이다.

| 주차 | 강의 | 포트폴리오 | 주말 산출물 |
| --- | --- | --- | --- |
| **1** | 섹션 1 (3강) + 섹션 2-1~2 | 저장소 초기화, Quarto 설치, 스택 세팅, CI 배포 파이프라인 | **빈 사이트라도 URL 확보** ← 가장 중요 |
| **2** | 섹션 2-3~4 (1D/2D) | 갤러리 `1d.ipynb`, `2d.ipynb`, `theme.py` 확정 | 갤러리 1차 공개 |
| **3** | 섹션 2-5~8 (시계열/복잡/차원축소/기타) | 갤러리 나머지 4개 + 의사결정 가이드 | **32기법 갤러리 완성** |
| **4** | 섹션 3 실습 전반 | 케이스 1 (Seoul Bike) | 케이스 1 공개 |
| **5** | 섹션 3 실습 후반 (센서/음파) | 케이스 2 (전력 시계열) | 케이스 2 공개 |
| **6** | 섹션 4 + 복습 | 케이스 3 (SECOM) | 케이스 3 공개 |
| **7** | — | 랜딩/About/README 다듬기, 색상 접근성 점검, 모바일 확인, 최종 리뷰 | **완성** |

**1주차에 빈 사이트라도 배포하는 이유:** 배포는 마지막에 몰면 반드시 막힌다.
(한글 폰트, CI 권한, 경로 문제) 내용이 없을 때 뚫어두면 이후엔 푸시만 하면 된다.

---

## 7. 품질 체크리스트

배포 전 매번 확인. 이직용이라 이 부분이 실력 차이로 보인다.

**차트**
- [ ] 제목이 결론 문장인가 (명사구가 아니라)
- [ ] 축 라벨에 단위가 있는가
- [ ] 색맹 안전 팔레트인가 (빨강-초록 대비 금지)
- [ ] 다크모드에서도 읽히는가
- [ ] 데이터 출처와 기간이 표기되어 있는가
- [ ] 범례가 필요한가, 직접 라벨링이 나은가

**재현성**
- [ ] `uv sync` + 데이터 스크립트만으로 처음부터 돌아가는가
- [ ] 랜덤 시드가 고정되어 있는가 (t-SNE/UMAP/K-means)
- [ ] CI에서 렌더가 성공하는가 (= 남의 컴퓨터에서도 돌아감)

**전달**
- [ ] 첫 화면에서 "이게 뭔지" 3초 안에 이해되는가
- [ ] 각 케이스가 액션 제안으로 끝나는가
- [ ] 모바일에서 표·차트가 깨지지 않는가

---

## 8. 알려진 함정

| 함정 | 대응 |
| --- | --- |
| CI(우분투)에 한글 폰트 없어 `□□□` 출력 | 워크플로에 `fonts-nanum` 설치 + `fc-cache -f` |
| 노트북 출력이 diff를 수천 줄로 만듦 | nbstripout — 단, **포트폴리오 저장소는 반대로 출력을 남긴다** (사이트 렌더 결과가 곧 산출물) |
| 200만 행 데이터를 git에 커밋 | `data/`는 gitignore, `src/datasets.py`가 다운로드+캐시 |
| Quarto 렌더가 매번 전체 재실행되어 느림 | `freeze: auto` 설정 — 소스가 안 바뀌면 캐시 사용 |
| UMAP 설치가 무겁고 느림 | 케이스 3에서만 쓰므로 optional dependency 그룹으로 분리 |
| 케이스가 "차트 전시"로 끝남 | 각 케이스에 `## 그래서, 무엇을 해야 하는가` 섹션 강제 |

---

## 9. 지금 바로 할 일 (1주차)

1. `data_visualization_inflearn`에 첫 커밋 (기존 작업 보존)
2. 이 폴더에 `git init` + `pyproject.toml` + `uv sync`
3. Quarto 설치 → `_quarto.yml` + `index.qmd` 뼈대
4. GitHub 저장소 생성 → Actions 워크플로 → **Pages URL 확보**
5. `src/theme.py`에 한글 폰트 + 팔레트 확정 (모든 차트가 이걸 쓰게)

---

## 10. 부록 — `data_visualization_inflearn` 노트북 품질 검토 (2026-08-16)

학습 트랙 노트북 8개(01~08, 코드 셀 44개)를 전수 검토한 결과. 결론: **학습 자료로는 상위권,
포트폴리오로는 그대로 못 쓴다.** 버릴 게 아니라 옮겨 담아야 할 자산이라 여기 남겨 둔다.

### 그대로 재사용 — 갤러리(§5) "흔한 실수" 슬롯 소스

대부분의 튜토리얼은 "이렇게 그린다"에서 끝나는데, 이 노트북들은 "이렇게 하면 틀린다"를 일관되게 짚는다.
갤러리 제작 시 아래를 거의 그대로 이식하면 §5 작업량이 줄어든다.

| 노트북 | 함정 |
| --- | --- |
| 03-2 | bin 개수가 결론을 바꾼다 (5/20/60 나란히 비교) |
| 03-4 | 박스플롯은 이봉분포(봉우리 두 개)를 숨긴다 |
| 04-3 | 막대 축을 0에서 안 시작하면 과장된다 (정직/과장 나란히 비교) |
| 05-7 | 이중 축은 상관관계를 조작해 보이게 한다 — 대안: 산점도 |
| 06-1 | 발산형 컬러맵은 `vmin=-1, vmax=1`로 0을 중심에 고정해야 |
| 06-3 | 버블 크기는 반지름이 아니라 면적으로 인코딩해야 (안 그러면 제곱 과장) |
| 06-5 | 레이더 차트는 축 순서 바꾸면 모양이 달라짐 — 순위 비교 부적합 |
| 07-1 | 표준화 빼먹으면 값 범위 큰 변수가 결과를 지배 |
| 07-4 | t-SNE는 군집 간 거리·크기·축값이 전부 무의미 (그래서 눈금을 지움) |
| 08-5 | AI 생성 차트 검증 체크리스트 8항목 |

### 포트폴리오로 그대로 못 쓰는 이유

1. **데이터가 전부 합성.** `viz_utils.load_sample()`이 `rng`로 생성 — 04-1의 상관계수 0.691도
   "점수에서 공부시간을 역산출"한 값. 학습용으론 맞는 선택(항상 같은 그림)이지만 채용 담당자에게
   "실제 데이터를 다룬다"의 증거는 안 됨. → §4 케이스 3개는 계획대로 공개 데이터셋으로 신규 제작.
2. **빈칸 다수.** 32기법 체크리스트 전부 미체크, "내 결론:", "내 메모", 연습문제 빈 셀, 8-6 정리표 공백.
   완성물이 아니라 템플릿 상태.
3. **커리큘럼 대비 커버리지 구멍.** 미실습: Swarm Plot, Waterfall, Slope Chart, 2D Density Plot,
   Residual Plot, K-means, Clustered Heat Map, NMF, UMAP, WordCloud, Radial Tree.
   **KDE는 노트북 목표에 적혀 있으나 실제 코드 없음** (03-3이 ECDF로 대체됨). 32개 중 11개 공백.
4. **seaborn 미사용.** 강의는 seaborn 기반인데 노트북은 순수 matplotlib, requirements.txt에도 없음.
   저수준 학습으로는 맞지만 코드가 김 (pair plot 20줄 vs `sns.pairplot` 1줄) —
   갤러리의 "10줄 이내" 목표(§5)와 충돌.
5. **색상이 시스템이 아님.** `#4C78A8`/`#E45756`류가 셀마다 하드코딩, `colors` dict가 06·07에 중복 정의.
   → `src/theme.py`(§3)에 팔레트를 만드는 이유가 바로 이거.
6. **체크리스트와 실천 불일치.** 8-5에 "색만으로 구분하지 않는가(색맹 대응)"를 적어 놓고
   06-3 버블·06-6 facet은 정작 색으로만 구분.

### 기술적으로 고쳐야 할 것

- **05-6 이상치 탐지가 자기참조적.** `thr = 3 * resid.std()`인데 `resid`에 이상치가 이미 포함된 채
  표준편차를 구해서 임계값 자체가 부풀려짐. MAD 기반(`1.4826 * median(|resid - median(resid)|)`)으로
  교체 필요 — **케이스 2(전력 시계열 이상탐지, §4)에서 제대로 다룰 것.**
- `requirements.txt`에 버전 핀 없음 (실측: pandas 3.0.5, numpy 2.5.1). `ax.boxplot(tick_labels=)`는
  matplotlib 3.9+ 필요. 재현성을 README에서 강조하면서 정작 언핀 — 포트폴리오 쪽은 `uv.lock`으로 해결(§2).
- `sys.path.append("..")` 반복 — 노트북 실행 위치에 의존.
- plotly `font_family="Malgun Gothic"` 하드코딩 — 리눅스 CI에서 깨짐 (§8의 한글 폰트 함정과 같은 종류).

### 미결정 사항 — §6 일정에 영향

32개 중 11개가 비어 있어 2~3주차 갤러리 작업량이 §6 계획보다 클 수 있음. 택1 필요:

- **(a) 11개 다 채운다** — §6 일정 재조정 필요 (2~3주차에 1주 추가 등)
- **(b, 권장) "실무에서 실제 쓰는 기법" 위주로 20개 안팎으로 범위를 좁힌다** — Radial Tree, NMF 등
  희귀 기법은 의도적으로 제외. 32개를 다 채우는 것보다 20개를 제대로 하는 쪽이 포트폴리오로는 낫다.

### 결론

학습 트랙(§1)은 이대로 유지 — 첫 커밋만 만들면 됨. 함정 해설은 갤러리로 이식, 합성 데이터 케이스는
포트폴리오에 쓰지 않고 공개 데이터셋으로 신규 제작(계획 변경 없음). 위 미결정 사항 (a)/(b)만 다음 세션에서
결정하면 됨.
